<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron needs these under the hardened runtime. -->
    <key>com.apple.security.cs.allow-jit</key><true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
    <key>com.apple.security.cs.disable-library-validation</key><true/>
    <!-- Without this the hardened runtime denies the camera outright and the
         eye-contact gate can never pass. Its absence is what broke the
         2026-09-04 build and forced a fallback to the unhardened backup. -->
    <key>com.apple.security.device.camera</key><true/>
</dict>
</plist>
